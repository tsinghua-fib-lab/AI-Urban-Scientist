# Socrata Platform Notes

Socrata datasets commonly expose a four-by-four view id such as `abcd-1234`. A platform helper can derive candidate export and catalog-search URLs, but the consuming SourceSkill or route runner still owns research-fit evidence.

Validation remains downstream:

- resolver
- probe
- download
- verifier

These notes must not be treated as an answer table or known-source substitute.
