# Dataverse Platform Notes

- Dataverse datasets often expose persistent ids such as DOI identifiers through API routes.
- Dataset metadata and file listings are not final downloadable resources.
- File access, terms, restricted files, versions, and checksums must be validated downstream.
- SourceSkills use this package only for platform mechanics after source-family fit is established.
