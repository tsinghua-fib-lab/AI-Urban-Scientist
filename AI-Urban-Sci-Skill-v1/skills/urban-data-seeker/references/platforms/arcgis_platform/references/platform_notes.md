# ArcGIS Platform Notes

ArcGIS REST services often expose `FeatureServer` or `MapServer` endpoints with optional numeric layer ids. A platform helper can construct metadata and query hints, but research-fit evidence belongs to the consuming SourceSkill or route runner.

Validation remains downstream:

- resolver
- probe
- download
- verifier

These notes must not be treated as an answer table or known-source substitute.
