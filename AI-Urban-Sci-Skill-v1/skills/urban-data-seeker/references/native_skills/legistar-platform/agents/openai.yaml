interface:
  display_name: "Legistar Platform"
  short_description: "Use Legistar legislative APIs"
  default_prompt: "Use $legistar-platform to route this urban data need to the right source or platform and list validation checks."

policy:
  allow_implicit_invocation: true
