interface:
  display_name: "CKAN Platform"
  short_description: "Use CKAN package/resource APIs"
  default_prompt: "Use $ckan-platform to route this urban data need to the right source or platform and list validation checks."

policy:
  allow_implicit_invocation: true
