interface:
  display_name: "Dataverse Platform"
  short_description: "Use Dataverse dataset APIs"
  default_prompt: "Use $dataverse-platform to route this urban data need to the right source or platform and list validation checks."

policy:
  allow_implicit_invocation: true
