interface:
  display_name: "OData Platform"
  short_description: "Use OData metadata and filters"
  default_prompt: "Use $odata-platform to route this urban data need to the right source or platform and list validation checks."

policy:
  allow_implicit_invocation: true
