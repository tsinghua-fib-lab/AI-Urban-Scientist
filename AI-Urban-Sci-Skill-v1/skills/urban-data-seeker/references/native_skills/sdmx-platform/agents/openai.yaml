interface:
  display_name: "SDMX Platform"
  short_description: "Use SDMX dataflow mechanics"
  default_prompt: "Use $sdmx-platform to route this urban data need to the right source or platform and list validation checks."

policy:
  allow_implicit_invocation: true
