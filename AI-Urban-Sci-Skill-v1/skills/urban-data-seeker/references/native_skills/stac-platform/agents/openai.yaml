interface:
  display_name: "STAC Platform"
  short_description: "Use STAC catalog mechanics"
  default_prompt: "Use $stac-platform to route this urban data need to the right source or platform and list validation checks."

policy:
  allow_implicit_invocation: true
